# AGENTS.md

## Cursor Cloud specific instructions

This repository (`StorageFacilityBuy`) is currently a greenfield project with no application code. It contains only a `README.md`.

### Current state

- **No application code** — no source files, frameworks, or entry points exist yet.
- **No dependency management** — no `package.json`, `requirements.txt`, `Cargo.toml`, or similar.
- **No tests, linting, or build configuration** — no CI/CD, no test frameworks, no linters configured.
- **No infrastructure** — no Docker, databases, caches, or external service dependencies.

### Environment

- Node.js 22.x and npm 10.x are available.
- Python 3.12 is available.
- Git 2.43 is available.

### Notes for future agents

Once application code is added, update this file with:
- How to install dependencies (update script).
- How to run lint, tests, and the dev server.
- Any non-obvious startup caveats or environment requirements.
